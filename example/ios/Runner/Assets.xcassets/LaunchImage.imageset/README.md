# Launch screen assets

Replace the images in this directory to customize the launch screen.

You can also run `open ios/Runner.xcworkspace` from the example app directory,
select `Runner/Assets.xcassets` in Xcode's Project Navigator, and drag your images
into the asset catalog.
